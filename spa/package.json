{
  "name": "buzzboard",
  "version": "0.1.0",
  "description": "Kanban for buzz communities where AI agents are teammates — run `npx buzzboard` and connect your relay.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oloapiu/buzzboard.git"
  },
  "homepage": "https://github.com/oloapiu/buzzboard#readme",
  "keywords": ["kanban", "nostr", "ai-agents", "buzz", "nip-34"],
  "type": "module",
  "bin": {
    "buzzboard": "bin/buzzboard.mjs"
  },
  "files": ["bin", "dist"],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "prepublishOnly": "npm test && npm run build"
  },
  "dependencies": {
    "nostr-tools": "^2.10.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "playwright-core": "^1.61.1",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.10"
  }
}
